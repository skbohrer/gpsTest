<?xml version="1.0" encoding="UTF-8" ?>
    <widget xmlns   = "http://www.w3.org/ns/widgets"
        xmlns:gap   = "http://phonegap.com/ns/1.0"
		xmlns:android = "http://schemas.android.com/apk/res/android"
        id          = "com.steadmanhill.gpstest"
        versionCode = "15"
        version     = "1.4.5" >

    <!-- versionCode is Android only: increase each build, unrelated to displayed version -->

		<name>GpsTest</name>

        <description>
			Test core geolocation plugin.
        </description>

        <author email="dev.steadman.hill@gmail.com">
			Steve Bohrer
		</author>

				<!-- preferences -->
		<preference name="permissions" value="none"/>			<!-- core plugins add own permissions -->
		<preference name='phonegap-version' value='cli-8.1.1' /> <!-- all: current version of PhoneGap -->
		<preference name="orientation" value="default" /> 		<!-- all: default means both landscape and portrait are enabled -->
		<preference name="target-device" value="universal" /> 	<!-- all: possible values handset, tablet, or universal -->
		<preference name="fullscreen" value="false" /> 			<!-- all: hides the status bar at the top of the screen -->
		<access origin="*" />

				<!-- Android -->
		<preference name="android-minSdkVersion" value="19" /> 			<!-- android: MIN SDK version supported. 19 == Android 4.4 kit kat -->
		<preference name="android-targetSdkVersion" value="26" /> 		<!-- highest version app was tested on. 26==8.0 -->

		<gap:platform name="android" />
		<plugin name="cordova-plugin-geolocation" spec="~4.0.2" />
</widget>
